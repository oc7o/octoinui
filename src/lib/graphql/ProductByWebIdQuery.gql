query productByWebId($webId: String!) {
	productByWebId(webId: $webId) {
		webId
		name
		slug
		description
		isActive
		createdAt
		updatedAt
		defaultImage {
			image
		}
		category {
			name
			slug
		}
		inventories {
			webId
			attributes
		}
	}
}
